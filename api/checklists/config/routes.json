{
  "routes": [
    {
      "method": "GET",
      "path": "/checklists",
      "handler": "checklists.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/checklists/count",
      "handler": "checklists.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/checklists/:id",
      "handler": "checklists.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/checklists",
      "handler": "checklists.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/checklists/:id",
      "handler": "checklists.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/checklists/:id",
      "handler": "checklists.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
